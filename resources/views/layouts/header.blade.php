<header class="w-full bg-base-100 shadow-md">
    <div class="max-w-6xl mx-auto px-4 py-4">
        <nav class="space-x-6">
            <a href="{{ route('home') }}" class="text-base-content hover:text-primary">Accueil</a>
            <a href="{{ route('projets') }}" class="text-base-content hover:text-primary">Projets</a>
            <a href="{{ route('competences') }}" class="text-base-content hover:text-primary">Compétences</a>
            <a href="{{ route('contact') }}" class="text-base-content hover:text-primary">Contact</a>
            <a href="{{ route('cv') }}" class="text-base-content hover:text-primary">CV</a>
        </nav>
    </div>
</header>
